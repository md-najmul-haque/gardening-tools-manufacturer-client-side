import React from 'react';

const Tool = ({ product }) => {
    const { name, img, description, price, available, minimumOrderQuantity } = product
    return (
        <div>

        </div>
    );
};

export default Tool;
